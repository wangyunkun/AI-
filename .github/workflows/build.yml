name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    # 安装 Linux 基础库 (解决之前那个 driver 报错)
    - name: Install Linux Libraries
      run: |
        sudo apt-get update
        sudo apt-get install -y libgtk-3-dev libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev mesa-utils

    # 👇👇👇 核心修改在这里 👇👇👇
    # 将版本号从 3.24.0 改为 3.27.0，满足 Flet 的要求
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.27.0'  # <--- 修改了这里！
        channel: 'stable'
        cache: true

    - name: Install Dependencies
      run: |
        pip install -r requirements.txt
        pip install flet --upgrade

    # 哪怕版本对上了，我们还是保留 "yes |" 作为双重保险
    # 万一它还要问别的问题，yes 会自动帮我们同意
    - name: Flet Build APK
      run: |
        yes | flet build apk . --verbose

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: safety-app-release
        path: build/**/*.apk
