name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Linux Libraries
      run: |
        sudo apt-get update
        sudo apt-get install -y libgtk-3-dev libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev mesa-utils

    # 👇👇👇 重点修改 1：回退到 Flutter 3.24.0 (配合 Flet 0.25.2) 👇👇👇
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.0'
        channel: 'stable'
        cache: true

    # 👇👇👇 重点修改 2：强制安装 Flet 0.25.2 (最稳定版本) 👇👇👇
    - name: Install Dependencies
      run: |
        pip install -r requirements.txt
        pip install flet==0.25.2

    - name: Flet Build APK
      run: |
        # 0.25.2 版本不需要 yes 也能跑，但保留着也没事
        yes | flet build apk . --verbose

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: safety-app-release
        path: build/**/*.apk
